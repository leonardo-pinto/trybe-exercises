const stringUpperCase = (str) => str.toUpperCase(str);

const firstCharacter = (str) => str.chartAt(0);

const concatenateString = (str1, str2) => str1.concat(str2);
  
module.exports = { stringUpperCase,
firstCharacter,
concatenateString  }
